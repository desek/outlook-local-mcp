Read and execute all instructions in docs/prompts/mcp-tool-crud-test.md step by step.

After completing the test run, write the full report (results table, environment table, findings, and any notes) to a timestamped file at the repository root named `TEST-REPORT-{YYYY-MM-DDTHH-MM-SS}.md` using the current local datetime (colons replaced with hyphens so the filename is filesystem-safe). Do not overwrite previous reports.
