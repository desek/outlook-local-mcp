# macOS metadata
.DS_Store

# sed temp files (macOS in-place edit side effects)
.!*

# Go binary artifacts
/outlook-local-mcp
/outlook-local-mcp-*
/cmd/outlook-local-mcp/outlook-local-mcp
*.exe
*.exe~
*.dll
*.so
*.dylib
*.log

# Go test binary artifacts
*.test

# Go coverage output
*.out

# SBOM artifacts
*.cdx.json
*.spdx.json

# Node modules (commitlint)
node_modules/

# Docker volume data
data/

# Environment-specific MCP config (contains local paths)
.mcp.json

# GoReleaser build artifacts
dist/

# MCPB extension build artifacts
extension/bin/
*.mcpb
.local/

# Local test reports
TEST-REPORT*.md

# CRUD bench per-run artifacts (CSV is checked in)
docs/bench/runs/

# CR-0060 bench scratch space
/bin/
/.worktrees/
docs/research/cr-0060-token-economics/runs/
# Staged container binaries produced by scripts/stage-container-binaries.sh
/linux/

# Claude Code agent worktrees
/.claude/worktrees/

# Vendored website source under evaluation, copied from
# ../experiments/saas-anything/output/outlook-local-mcp-v3. Held out of version
# control deliberately: it is a reference copy being assessed, not yet the
# project's site. Promote it by moving it to a tracked path and removing this
# entry, rather than by committing it in place from here.
/site-v3/
