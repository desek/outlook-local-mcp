// Package tools provides MCP tool definitions and handler constructors for the
// Outlook Calendar MCP Server. Each tool handler interacts with the Microsoft
// Graph API to perform calendar operations (list, get, search, create, update,
// delete, cancel, respond, reschedule) and their meeting variants for
// attendee-focused operations. Tool definitions and handlers are registered by
// the server package during startup.
package tools
